# Noisemaker v1.0.104 Artistic Effects Sync Implementation Plan

> **Execution:** Use `superpowers:subagent-driven-development` in this session. Each task is implemented test-first, committed independently, reviewed against its task brief, and only then marked complete in `.superpowers/sdd/progress.md`.

**Goal:** Bring `noisemaker-unity` / `com.noisemaker.hlsl` from the previously verified upstream baseline `noisemaker@29725b18` to upstream `noisemaker@75507112` (feature tag `v1.0.104` at `1a29d431`), including all 25 new artistic filters, the nine changed shader paths, current runtime semantics, generated definitions, Shader Graph mirrors where the package already promises them, and graph/pixel parity evidence.

**Architecture:** The JavaScript Noisemaker repository is the executable specification. Preserve render-graph structure byte-for-byte in the C# compiler, port each WGSL program into an HLSL include plus Unity shader wrapper, bind definition `defines` as the package's existing uppercase runtime integer uniforms, and preserve declared intermediate formats. Fix target-only runtime pooling and repeat execution without changing the reference liveness allocation graph.

**Tech stack:** Unity 6000.3.16f1 Built-in pipeline, HLSL shader model 4.5, C# runtime/compiler, Node 22 definition/graph tools, .NET 8 graph dumper, Playwright WebGL2 goldens, Python/Pillow/numpy image comparison.

## Global constraints

- Work only in `/Users/alex/source/.codex-worktrees/noisemaker-unity-v104` on branch `codex/noisemaker-v104-sync`; the source repository is read-only and its untracked files must remain untouched.
- Upstream `/Users/alex/source/noisemaker` at `75507112` is authoritative for definitions, pass ordering, WGSL arithmetic, coordinate systems, sampler behavior, formats, and default values.
- Render surfaces and synthesized/intermediate textures are point-filtered and clamp-to-edge at current upstream HEAD; imported external media remains linear-filtered. Do not reintroduce blanket bilinear sampling.
- Respect every definition texture format. The new artistic multipass effects use `rgba8unorm` intermediates where declared; watercolor state is `rgba16f`. Do not normalize them all to ARGBHalf.
- Do not change `Compiler/Lang/Resources.cs` allocation IDs or graph structure to solve runtime aliasing. Add a runtime physical-format/is3D guard so graph parity remains byte-clean.
- Repeated global-surface passes must adopt the already-advanced frame-local bindings after each execution, matching upstream `pipeline.js::adoptIterationBindings`; never recompute iteration bindings by swapping a stale persistent record.
- WGSL fragment coordinates are already canonical top-left coordinates. Preserve global/tile coordinate transforms and explicit `SampleLevel(..., 0)` requirements exactly.
- Definition JSON is generated by `tools/convert-definitions.mjs`, not hand-written. New Unity `.meta` files must be deterministic and committed.
- Every runtime shader uses `Blend Off`, `#pragma target 4.5`, and the existing GLES exclusion. Pass names exactly match definition program basenames so `Shader.Find("Noisemaker/<namespace>/<func>")` and named-pass lookup work.
- Multi-pass filters are runtime-only. Add/update Shader Graph Custom Function wrappers for the package's existing single-pass filter coverage: changed wrappers for Dither, Edge, Emboss, Invert, LowPoly, Parallax, Texture, Mandala, and SacredGeometry; new wrappers only for tractable single-pass filters that follow existing conventions. Do not create Shader Graph wrappers for multi-pass effects, Median, Halftone, or other compiler-risk/high-cost paths.
- No completion claim without: clean .NET build, zero-delta graph parity for the complete self-test plus targeted v1.0.104 cases, clean Unity import/compile, and pixel comparison against fresh WebGL2 goldens for every new effect plus every new disjoint branch listed below.

## Task 1: Add the v1.0.104 parity corpus and prove it starts red

**Files:**

- Add: `parity/programs/v104/*.dsl`
- Add: `parity/programs/v104/manifest.tsv`
- Modify: `parity/README.md`

Create deterministic, fixed-seed DSL programs at 127×127 parity size using `testPattern` inputs. Include at least one case for every new effect and explicit branch cases for:

- `directionalBlur`; `hatch` colored-pencil; `stipple` mezzo-strokes and reticulation; `strokes` sumi-e and smudge; `oilPaint` sponge with size 10.5; `stamp` roughness path.
- `extrude` pyramids/random/non-solid and blocks/luminance; `lensFlare` movie-prime, prime35, and prime105; `mosaicTiles` shifted/inverse, shifted/unaltered, and mosaic/relief; `pondRipples` repeat/no-AA and clamp/AA; `patchwork`; `wind` stagger/from-right, wind, and strength-zero passthrough.
- `relief` plaster and note-paper; `halftone` color plus mono dot/line/circle; `watercolor`; `craquelure`; `plasticWrap` nonzero and zero light vectors; `scatter` anisotropic, darken-only, lighten-only, and clumped; `chrome` distorted and zero-distortion; `photocopy`; `morphology` round/erode and square/dilate; `median` radii 1, 2, 3 and threshold-zero; `highPass` radius above 32 and mono; `unsharpMask` radius above 32; `spinBlur` off-center.
- Existing changes: `dither` error-diffusion; `edge` contour/lower and contour/upper; `emboss` color and gray styles; `invert` solarize; `lowPoly` border+lighting; each new `texture` mode 5–14 with non-default intensity/contrast/mono; large-format/tile-aware `parallax`, `mandala`, and `sacredGeometry`.

The manifest format is `<name><TAB><absolute-or-repo-relative-dsl-path>` and must list every v1.0.104 file exactly once. Document the new corpus in `parity/README.md`.

Run the reference exporter against all cases to prove the programs themselves are valid, then run the C# graph dumper against the unmodified target definitions. Expected RED: new-effect programs fail as unknown effects and changed-definition cases differ. Record the exact failure count in the task report; do not weaken or delete cases.

Commands (using the bootstrapped runtimes):

```bash
PATH=/tmp/nm-node.SoOIaC/nodeenv/bin:$PATH NM_REFERENCE_ROOT=/Users/alex/source/noisemaker node tools/export-graph.mjs --file parity/programs/v104/<case>.dsl /tmp/<case>.ref.json
PATH=/tmp/nm-dotnet.FoaV14:$PATH dotnet build -c Release tools/graphdump
```

Commit: `test: add v1.0.104 artistic parity corpus`

## Task 2: Port repeat bindings and make runtime pooling format-safe

**Files:**

- Modify: `unity/com.noisemaker.hlsl/Runtime/Pipeline/SurfaceManager.cs`
- Modify: `unity/com.noisemaker.hlsl/Runtime/Pipeline/NMPipeline.cs`
- Modify: `unity/com.noisemaker.hlsl/Runtime/Pipeline/TextureStore.cs`
- Modify: `PORTING-GUIDE.md`

Replace `SwapIterationBuffers(Pass)` with `AdoptIterationBindings(Pass)`: after `UpdateFrameSurfaceBindings` advances `_frameRead` and `_frameWrite`, copy those frame-local IDs into each affected persistent `SurfaceRecord.Read/Write`. Call adoption after every repeated pass execution and at DSL loop-bracket iteration boundaries; preserve the existing skip behavior and end-of-frame swap.

Extend `NMPipeline.ResolvePhysical`'s alias guard. In addition to logical width/height, compare the logical mapped render format (`TextureStore.MapFormat(s.Format)`) and `s.Is3D` against the pooled `RenderTexture`. If any differ, create/reuse a dedicated texture keyed by virtual ID. Make `TextureStore.CreateOrReuse` include mapped format in its reuse predicate, and make `SurfaceManager.CreateSurfaces` include mapped format when deciding whether persistent read/write surfaces can be retained. Update `TextureStore.AllocatePooled` comments so they no longer claim all mapped IDs share a format.

Update `PORTING-GUIDE.md` to state current point/clamp render-surface sampling, linear imported media, per-definition formats, `rgba8unorm` quantization, and frame-local repeat adoption.

Verification: clean `dotnet build -c Release tools/graphdump`; all pre-v1.0.104 graph programs remain zero-delta. Pixel RED for watercolor remains until Task 6 adds its shader, but code review must trace seed → simplify iteration 1 → simplify iteration 2 bindings and show that iteration 2 reads iteration 1's write.

Commit: `fix: align repeat bindings and texture formats`

## Task 3: Port the changed pre-existing effects

**Files:**

- Modify runtime HLSL and `.shader` files for `filter/dither`, `filter/edge`, `filter/emboss`, `filter/invert`, `filter/lowPoly`, `filter/texture`, `filter/parallax`, `synth/mandala`, and `synth/sacredGeometry` under `unity/com.noisemaker.hlsl/Shaders/Effects/`.
- Modify matching files under `unity/com.noisemaker.hlsl/ShaderGraph/CustomFunctions/`.
- Regenerated JSON is deferred to Task 7.

Port only the authoritative WGSL changes since `29725b18`:

- Dither: error-diffusion mode 7, including the exact blockwise Floyd–Steinberg apron/register algorithm and PCG coordinate seeding.
- Edge: contour kernel, `level`, `contourSide`, and render-scale-aware radius.
- Emboss: STYLE color/gray branches, angle/height/colorAmount, global/tile sampling, and default-color arithmetic identity.
- Invert: runtime `mode` and solarize branch `min(rgb, 1-rgb)`.
- LowPoly: `LP_BORDER` nearest-site border search and `LP_LIGHT` exposure lighting while preserving the original 3×3 default result.
- Texture: modes 5–14 and intensity/contrast/mono with exact upstream material-texture helpers.
- Parallax: global-to-local mapping and the 256-pixel large-format shift clamp only for nonzero tile offsets.
- Mandala and SacredGeometry: divide global position by `fullResolution`, not local resolution.
- Confirm `grain` already matches the upstream global-coordinate change; do not introduce a no-op diff.

Run reference export + C# graph dump on the changed-effect v1.0.104 DSLs; graph differences are expected until definitions regenerate. Run Unity compile after this task and reject shader compiler errors/warnings attributable to the changed files.

Commit: `feat: sync changed effects through v1.0.104`

## Task 4: Port new single-pass artistic filters, group A

**Files:**

- Add runtime `.hlsl`, `.shader`, and `.meta` pairs for `directionalBlur`, `hatch`, `stipple`, `extrude`, `lensFlare`, `mosaicTiles`, `pondRipples`, `patchwork`, and `wind` under `unity/com.noisemaker.hlsl/Shaders/Effects/filter/`.
- Add Shader Graph wrappers and `.meta` files only for `directionalBlur`, `hatch`, `stipple`, `lensFlare`, `mosaicTiles`, `pondRipples`, and `patchwork` under `unity/com.noisemaker.hlsl/ShaderGraph/CustomFunctions/`.

Translate each upstream WGSL program line-for-line in arithmetic and control flow. Preserve uppercase runtime define uniforms (`MODE`, `PATTERN`, `EXTRUDE_TYPE`, `DEPTH_SOURCE`, `LENS_TYPE`, `STYLE`, `WRAP`, `METHOD` as applicable), explicit LOD reads in nonuniform loops, global/local coordinate conversions, HLSL `ddx`/`ddy` for pond ripples, floor-based modulo for wrap logic, and all literal iteration bounds.

Use the Task 1 programs as RED tests before adding shaders. After implementation, run a temporary Unity project import/compile and render this task's candidates against fresh WebGL2 goldens. Exact tolerance target is max channel delta ≤1/255 and SSIM 1.0; any documented near-parity exception requires pixel-localized evidence rather than a blanket tolerance increase.

Commit: `feat: add single-pass artistic filters`

## Task 5: Port new stroke and paint multipass filters

**Files:**

- Add runtime `.hlsl`, `.shader`, and `.meta` pairs for `strokes`, `oilPaint`, `stamp`, `chrome`, `photocopy`, `plasticWrap`, `relief`, `highPass`, and `unsharpMask` under `unity/com.noisemaker.hlsl/Shaders/Effects/filter/`.

Preserve exact definition pass names/order and bind every declared `rgba8unorm` temporary. Keep fixed sample bounds, branch/tie behavior, global coordinate hashes, blur radius mappings, source-alpha rules, and explicit LOD reads from nonuniform branches. No Shader Graph wrappers are created for these multipass effects.

Use Task 1 cases as RED tests before shader creation. Run Unity compile and task-scoped fresh golden/candidate pixel comparisons after implementation. Treat visible 8-bit inter-pass quantization as required output, not noise.

Commit: `feat: add stroke and paint filter passes`

## Task 6: Port remaining new artistic filters and feedback-sensitive watercolor

**Files:**

- Add runtime `.hlsl`, `.shader`, and `.meta` pairs for `watercolor`, `halftone`, `craquelure`, `scatter`, `morphology`, `median`, and `spinBlur` under `unity/com.noisemaker.hlsl/Shaders/Effects/filter/`.
- Add Shader Graph wrappers and `.meta` files only for `craquelure` and `spinBlur`.

Preserve:

- Watercolor's rgba16f global state, exact nine-sample/19-compare componentwise sorting network, `repeat: 2`, Sobel signs, grain coordinates, and original alpha. Its pixel case is the acceptance test for Task 2 repeat adoption.
- Halftone global screen coordinates, explicit rotation, CMYK gathers, all mono patterns, strict comparisons, and derivatives.
- Scatter's global half-pixel hashes, anisotropic Sobel projection, componentwise RGBA min/max modes, and 3×3 tent pass.
- Morphology's separable square path, capped nonseparable round path, runtime mode, uppercase `SHAPE`, and fractional-radius inclusion.
- Median's fixed-capacity 49-record runtime implementation, exact `asuint` brightness keys, half packing/unpacking with `f32tof16`/`f16tof32`, initialized-range quickselect, center alpha, and threshold condition. Do not substitute a float sort.
- Craquelure and spin blur global coordinate/sample transforms exactly.

Use Task 1 cases as RED tests before shader creation. Compile in Unity before pixel rendering, with median treated as the primary Metal compiler-risk gate. Run task-scoped fresh golden/candidate comparisons, including all median radii and watercolor's full eight-frame run from cleared state.

Commit: `feat: add remaining artistic filters`

## Task 7: Regenerate definitions and prove complete graph parity

**Files:**

- Modify generated JSON under `unity/com.noisemaker.hlsl/Effects/`.
- Add generated JSON and `.meta` files for all 25 new filters.
- Update: `parity/corpus/` from the current reference `--selftest` output.
- Modify: `parity/README.md` counts/status.

Run `tools/convert-definitions.mjs` against `/Users/alex/source/noisemaker@75507112`. Verify exactly 210 definition JSON files are present and generation reports zero failures. Generate/update deterministic Unity `.meta` files without changing existing GUIDs.

Refresh the source self-test corpus using the repository's existing export workflow. Build the .NET graph dumper, export reference graphs for the entire self-test corpus plus `parity/programs/**/*.dsl`, dump C# graphs in one manifest run, and require zero structural deltas. Update README counts from measured results only.

Commands:

```bash
PATH=/tmp/nm-node.SoOIaC/nodeenv/bin:$PATH NM_REFERENCE_ROOT=/Users/alex/source/noisemaker node tools/convert-definitions.mjs
PATH=/tmp/nm-dotnet.FoaV14:$PATH dotnet build -c Release tools/graphdump
PATH=/tmp/nm-dotnet.FoaV14:$PATH dotnet tools/graphdump/bin/Release/net8.0/graphdump.dll unity/com.noisemaker.hlsl/Effects /tmp/v104-graph-manifest.tsv
python3 parity/graph-diff.py <reference.json> <candidate.json>
```

Commit: `chore: regenerate v1.0.104 effect definitions`

## Task 8: Run full Unity compile and pixel-parity gate

**Files:**

- Modify only if verification uncovers a real defect: affected runtime/shader/test files.
- Add no generated PNGs, logs, `Library/`, `Temp/`, or absolute-path manifests to git.

Create a disposable copy of `/Users/alex/nmhlsl-unity` under `/tmp`, excluding `Library`, `Temp`, `Logs`, and `obj`, and point its local package dependency at this worktree. Do not modify the user's existing Unity project.

Run one Unity batch import/compile and inspect the full Editor log for shader/C# errors. Generate fresh WebGL2 goldens for the entire v1.0.104 manifest in one browser session, render all candidate graphs in one Unity session with `NMParityRunner.RenderBatchFromCommandLine`, and compare every pair with `parity/batch-compare.py`. Retain exact/near statistics and any per-effect exception evidence in the task report, but keep outputs outside git.

Also re-run the original 20 pixel programs to prove no regression against the prior 18 exact + 2 documented near baseline. Then run:

```bash
git diff --check
git status --short
PATH=/tmp/nm-dotnet.FoaV14:$PATH dotnet build -c Release tools/graphdump
```

Fix and re-run the narrow failing case first, then the complete affected batch. Completion requires zero missing shaders, zero Unity compile errors, zero graph deltas, every new effect rendered, and every pixel case within its evidence-backed threshold.

Commit any verification fixes as `fix: close v1.0.104 parity gaps` (omit the commit if no source changes were required).

## Task 9: Whole-branch review and delivery

Use `superpowers:requesting-code-review` with a review package from branch merge-base `2ab966e` through HEAD. The reviewer must inspect runtime semantics, format preservation, shader/definition pass-name agreement, test breadth, absence of generated artifacts, and the final verification report. Dispatch one fix worker for all Critical/Important findings, re-run covering tests, and repeat review until clean.

Then use `superpowers:verification-before-completion` and `superpowers:finishing-a-development-branch`. Do not push or mutate `/Users/alex/source/noisemaker`; report the ready branch/worktree, commits, exact verification counts, and any narrowly documented pixel tolerances.
